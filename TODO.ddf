dataType is never used, why documented as required?
autoFocus is never documented, but used
validateOnMount "

inconvenient validators syntax

no docs on tabs

why do we always need to cleanVirtualDom?

how to get converted schema ($r.schema is the original, unconverted)

tabs look bad because the %ul has an extra .nav-tabs-pf (removing that class fixes it)

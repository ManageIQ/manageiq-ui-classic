/ begin: app/views/layouts/listnav/_timeline.html.haml
- if @timelines_tree
  .panel-group#accordion
    = miq_accordion_panel(_("Timelines"), true, "timelines_tree") do
      = render(:partial => "shared/tree", :locals  => {:tree => @timelines_tree, :name => @timelines_tree.name})
/ end: app/views/layouts/listnav/_timeline.html.haml

- if @ght_type == 'data'
  = react('ReportDataTable', {:reportResultId => @report_result_id,
                              :perPageOptions => pp_options,
                              :perPageDefault => @settings[:perpage][:reports],
                              :reportType => @ght_type})
- else
  #report_html_div= update_content(@html, :striped, 'preview').html_safe

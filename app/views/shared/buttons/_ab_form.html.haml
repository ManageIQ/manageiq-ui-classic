= render :partial => "layouts/flash_msg"
= react('AbForm', :recId               => @custom_button.try(:id),
                  :appliesToClass      => @sb[:applies_to_class].to_s,
                  :appliesToId         => @sb[:applies_to_id].to_s,
                  :customButtonGroupId => @sb[:button_group_id],
                  :formDataUrl         => url_for_only_path(:action => 'ab_button_form_data'),
                  :redirectUrl         => url_for_only_path(:action => 'explorer'))

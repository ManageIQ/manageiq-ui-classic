%diagnostics-database-form-component{'diagnostics-database-id'             => @id,
                                     'submit-url'                          => "/#{controller_name}/db_backup/",
                                     'db-backup-form-field-changed-url'    => "/#{controller_name}/db_backup_form_field_changed/",
                                     'sb'                                  => @sb[:active_tab].to_json,
                                     'database-display-name'               => @database_display_name,
                                     'database-details'                    => @database_details.to_json,
                                     'backup-schedules'                    => @backup_schedules.invert.sort_by { |name, _id| name }.to_json,
                                     'database-backup-options-for-select'  => @database_backup_options_for_select.to_json}
:javascript
   miq_bootstrap('diagnostics-database-form-component')


- if x_node == "xx-u"
  - if @view

    = react 'RbacModule'

    :javascript
      $(document).ready(function() {
        sendDataWithRx({
          type: 'init-react-routing',
          reactRouting: true,
        })
      });

#main_div
  - if ManageIQ::Providers::StorageManager.any?
    = render :partial => 'layouts/gtl'
  - else
    = render :partial => 'layouts/empty',
             :locals => {:add_message => _("Add a new Object Storage Manager"),
                         :documentation => ::Settings.docs.object_manager}

- if %w(storage_system).include?(@display)
  = render :partial => "layouts/gtl", :locals => {:action_url => "show/#{@record.id}"}
- else
  - if @showtype == "item"
    = render :partial => "layouts/item"
  - else
    = render :partial => "layouts/textual_groups_generic"
